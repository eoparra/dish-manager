import type { CategoryName } from './types';

export const CATEGORIES: CategoryName[] = ['Fruit shop', 'Butchery', 'Supermarket'];

export const MAX_SELECTED_DISHES = 10;

export const ADMIN_PASSWORD = 'dishadmin'; // change this to your preferred password
export const ADMIN_AUTH_STORAGE_KEY = 'dish-manager-admin-auth';
